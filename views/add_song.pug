extends layout

block content

    h1 Add Song
    form(method='POST', action='/songs/add')
      #form-group
        label Name:
        input.form-control(name='name', type='text' ,required='')
      br
      #form-group
          label Artist:
          input.form-control(name='artist', type='text',required='')
      #form-group
          label Artist:
          input.form-control(name='user', type='hidden',required='', value=user.id)

      br
      input.btn.btn-primary(type='submit',value='Submit')
